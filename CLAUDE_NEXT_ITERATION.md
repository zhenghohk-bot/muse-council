# Muse Council 下一阶段交接

## 你的角色

你正在接手 Muse Council 当前分支上的下一轮迭代。请同时以资深 AI 产品经理、Harness Engineer 和 LLM Evaluation Engineer 的视角工作。

项目路径：

```text
/Users/hejennifer/Sites/muse-council
```

当前分支：

```text
agent/p2-evaluation-case-study
```

## 重要边界

1. 当前工作区不是干净的，包含用户、Codex 和此前 Claude Code 共同完成但尚未提交的改动。不得 reset、checkout、stash、覆盖或删除这些改动。
2. 不读取、打印、修改或提交 `.env.local`。
3. 不运行完整 API 评测，不开启后台循环，不重复消耗 Token，除非用户明确确认。
4. 不针对单个题目堆关键词补丁。每次修改都要说明它解决的是哪一类问题、位于哪一层 Harness。
5. 安全、事实、来源和状态合法性可以硬校验；长度、语气、修辞、人物风格等体验指标应优先作为 repair 或评测信号，不能继续变成大量固定台词。

## 当前已经完成

### 产品与 Harness

- Next.js 圆桌 MVP：提问、选先行者、分阶段谈话、追问、收束、行动卡、赠言卡。
- `QuestionIntent` 与 `SupportMode` 分离：任务意图和情绪信息不再混成一个分类。
- 表达技能题可识别为 `skill_building`，不再默认塞入情绪支持模式。
- 主持人开场必须介绍全部入席者，并禁止虚构她们经历过用户的相似处境。
- 第一轮发言不再使用统一固定前缀，发言长度以语义完整为先，最多 220 字，可自然拆成一至两个气泡。
- 用户追问可识别 question、reflection、commitment、closure、correction 等状态；普通判断问题不再被误当成反驳。
- 讨论不强制交锋，可根据意图选择递进、补充、澄清、转向或跳过。
- 行动卡的“本轮选择”已改为“本轮练习路径”，允许组合多位先行者互补的环节，不评选单一赢家。
- 先行者之间可在真实承接时使用圆桌短名，例如“清照”“简”“阿达”。

### 三位 PioneerMind 样板

以下三位已有能力、推理、互动、当代映射和人物专属 fallback：

- 李清照
- 简·奥斯汀
- 阿达·洛夫莱斯

相关文件：

- `lib/harness/pioneer-mind.ts`
- `lib/harness/question-intent.ts`
- `data/pioneers.ts`
- `lib/harness/stage-generator.ts`

### 最新一轮修复

针对人工体验中的共同问题，已经完成：

- 主持人只介绍人物观察角度，不展开每人的详细任务。
- 表达题的角度压缩为“李清照看重点与字句、简看听者需要、阿达看练习方法”这一层。
- 简不得使用“从你的话里拿走什么”“对方要用哪一样”等产品术语，改用“先听懂什么、哪里需要补充”。
- 阿达不得把反馈练习表述为“把判断责任推给听者”，反馈只用于发现下一处修改。
- 上述两类表达进入 Output Guard：先触发 repair，仍不合格才进入人物专属 fallback。
- `npm run check:harness`、`npx tsc --noEmit`、`npm run build` 已通过。

## 当前不要直接做的事

- 不要马上跑旧 8 题全量评测。旧题已经反复参与 Prompt 调整，主要是开发集，不足以证明泛化。
- 不要立即扩写九位人物。先确认三位样板在真实对话中达到可接受水平，再复制架构。
- 不要继续添加固定开头、固定回答结构或更短的字数限制。
- 不要因为一个 LLM 裁判低分就直接改 Prompt。

## 下一步顺序

### Phase A：三题定向人工回归

由用户在浏览器中完成。每题进行 1–2 次追问，并生成行动卡与赠言卡。

#### 题目 1：判断标准

```text
怎么判断是我没说清楚，还是对方不熟悉这个话题？
```

建议追问：

```text
那我现在可以先做什么，才能判断自己的表达有没有变清楚？
```

#### 题目 2：混合意图

```text
我一到汇报就特别紧张，脑子一片空白，怎么提升表达能力？
```

建议追问：

```text
紧张的时候我根本来不及组织完整的话，该从哪里开口？
我明白了，那我可以如何锻炼呢？
```

#### 题目 3：准备很多但重点不清

```text
我认真准备了很多内容，但说出来总让人抓不住重点，应该怎样练习？
```

建议追问：

```text
那我下一次表达前，具体应该怎样准备最重要的那一句？
好的，我明白了。
```

人工检查：

- 主持人是否提到全部三人，并只说角度。
- 是否准确围绕用户当前问题，没有补写用户未说出的情绪和行为。
- 三位人物是否提供不同而互补的判断方式。
- 简和阿达是否仍会互换人格或掉入相同 fallback。
- 方法是否解释目的、步骤和判断是否有效的方式。
- 追问是否回答最新意图，closure 是否简短结束。
- 行动卡是否来自整场谈话，练习路径是否前后连贯。
- 赠言是否忠实于本场发言，并具有角色特色。

只有 Phase A 的主要问题通过，才进入 Phase B。

### Phase B：评测 Harness v2

先设计，再实现。不要调用模型 API，除非用户确认。

需要把题集拆成：

1. `dev`：日常调试，可见题目和失败原因。
2. `regression`：沉淀真实失败模式，验证已修复能力。
3. `hidden`：版本验收使用，不参与 Prompt 调试。
4. `ood`：分布外自由问题，例如纯技能、知识探索、创作讨论和混合意图。

需要保留三条评测轨道：

1. 确定性检查：结构、来源、状态、明显复读、未授权推断、卡片溯源。
2. LLM 质量评测：读题、人物区分、推进、贴题、语言、行动卡、赠言。
3. 人工体验：参与感、活人感、继续追问意愿、卡片保存意愿、UI 节奏。

需要新增：

- 人物盲测：遮住姓名，让裁判猜人物并说明证据。
- A/B 配对盲测：同题比较旧 Harness 与新 Harness，裁判不知道版本。
- 生成稳定性记录：至少报告中位数、最差案例、fallback 率和硬失败率，不只报平均分。
- 裁判元数据：端点、模型 ID、Prompt 版本、日期。
- 冻结输出重复裁判，用于测量 GLM 裁判方差。

先提交设计文档和数据结构，不要一上来跑全量。

### Phase C：扩展九位 PioneerMind

三位样板通过后，把同一架构扩展到其余六位：

- 班昭
- 秦良玉
- 武则天
- 玛丽·居里
- 弗洛伦斯·南丁格尔
- 弗吉尼亚·伍尔夫

每位至少包含：

1. 能力层：擅长的问题意图、可提供的产出、明确边界。
2. 推理层：观察顺序、核心区分、证据标准、改变判断的条件、盲点。
3. 知识层：经过核验的作品、生平事件、历史原话和当代映射边界。
4. 人格与语言层：语气、节奏、坚定程度、提问方式、修辞额度、禁用模式。
5. 互动层：何时赞同、质疑、补充、让步、保持沉默。
6. fallback 层：按用户意图选择的人物专属思维动作，不是固定台词。

人物间互动必须由本场意图和对话状态决定。允许赞同、强烈反对、补充或转向，但不能强制制造碰撞，也不能复述对方后再表态。

### Phase D：完整回归与作品集证据

完成前三阶段后再运行：

```bash
npm run check:harness
npx tsc --noEmit
npm run build
```

模型评测应先跑小样本，再由用户确认是否运行完整题集。最终输出：

- 三轨评测结果。
- 人物盲测结果。
- 典型失败及修复前后对照。
- baseline、模型与裁判元数据。
- 已知限制。
- 可用于 AI 产品经理简历与 Case Study 的结论。

## 仍未完成的产品事项

这些事项已讨论过，但不是当前生成质量回归的下一步：

- 首页超宽屏标题溢出。
- 登录、历史记录、圆桌恢复和卡片收藏。
- 九位人物完整知识库扩增。
- 大型向量 RAG。
- 线上部署、额度限制和支付。

不要把它们与当前三题回归混在同一批修改里。

## 开始时应做什么

1. 运行 `git status --short --branch`，确认并保留现有改动。
2. 阅读本文件、`docs/ai-product-evaluation-learnings.md`、`docs/evaluation-v0.5.md`。
3. 先向用户复述当前状态和 Phase A–D 顺序。
4. 在用户提交三题人工结果前，只能准备 Phase B 的评测设计与数据结构方案，不得继续修改生成 Prompt。

