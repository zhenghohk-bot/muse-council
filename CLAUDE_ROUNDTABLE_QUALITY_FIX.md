# Muse Council 圆桌生成质量系统修复

你正在接手 Muse Council 的一轮系统质量修复。请以资深 AI 产品经理、Harness Engineer、中文内容编辑和 LLM Evaluation Engineer 的视角工作。

项目路径：

```text
/Users/hejennifer/Sites/muse-council
```

当前分支应为：

```text
agent/p2-evaluation-case-study
```

## 0. 工作边界

1. 工作区已有用户、Codex 和此前 Claude Code 的未提交改动。不得 `reset`、`checkout`、`stash`、覆盖或删除现有改动。
2. 不读取、打印、修改或提交 `.env.local`。
3. 先阅读：
   - `CLAUDE_NEXT_ITERATION.md`
   - `docs/ai-product-evaluation-learnings.md`
   - `docs/evaluation-v0.5.md`
   - 当前 Harness 相关实现
4. 先运行 `git status --short --branch`，再复述你对现状、根因和修改顺序的理解。随后直接实施，不要停在泛泛方案。
5. 不针对三道题添加关键词补丁、题目特判或固定答案。所有修改必须解决跨问题复现的系统根因。
6. 不降低现有安全、事实、来源和状态合法性要求。
7. 不运行旧题全量 API 评测，也不启动后台循环。完成代码后只运行无模型调用的检查；需要模型评测时先汇报并等待用户确认。

## 1. 产品目标

圆桌要同时做到：

- 准确理解用户此刻真正的问题，不补写用户未说出的感受、经历、行为或意图。
- 三位先行者有不同但互补的观察方式、知识和人格，不像共用一套模板。
- 发言自然、清楚、有思想和文学趣味；精简服从于表达完整，不以压字数为目标。
- 先行者之间能自然承接、补充、质疑或推进，但不强制交锋，不复述前文后再表态。
- 追问必须回答用户最新意图，不把普通提问误判成反驳，不把“我明白了”当作继续分析的邀请。
- 行动卡和赠言卡忠实于整场谈话，具体、可执行、可溯源。

## 2. 本轮人工回归暴露的系统问题

三道回归题分别是：

```text
1. 怎么判断是我没说清楚，还是对方不熟悉这个话题？
2. 我一到汇报就特别紧张，脑子一片空白，怎么提升表达能力？
3. 我认真准备了很多内容，但说出来总让人抓不住重点，应该怎样练习？
```

它们只是 regression 样本，不是产品只服务这三题。

### 2.1 主持人

- 开场有时只介绍两位先行者，或虽然介绍三位，但展开了过长、过细的内部任务分工。
- 把 Harness 的内部 `tension` 当成用户意图复述，例如用户只问表达训练，却被扩写成“在信息完整、重点突出与听者理解之间取舍”。
- 出现未经用户提供的判断或虚构性概括。
- 中文不自然，出现杂糅句式，例如“你要把认真准备的内容，让听者更容易听懂重点”。
- 开场与人物分工之间过渡生硬。

### 2.2 先行者发言与互动

- 简和阿达有时像可互换角色；阿达会掉入跨题重复的通用“小循环” fallback。
- 出现生硬或产品化表达，例如“对方要用的那一样”“从你话里拿走什么”“线索库”等。
- 出现不必要的道德化或矫枉过正，例如“不要把判断责任推给听者”。
- 后发言者虽然表面承接前文，实际逻辑跳跃，直接进入方法。
- 用户再次问到已回答内容时，会原句或近义复读；当前 fallback 替换后没有再次做跨人物、跨阶段重复检测。
- 用户只是询问“如何判断”，角色却误以为用户在反驳并认错。
- 用户明确表示“我明白了”时，角色仍输出一段新的抽象解释。
- 部分建议给了动作，却没有说明动作解决什么、如何判断有效。
- 过度限制字数导致语义没说完整；但也不能因此放任冗长。

### 2.3 行动卡与赠言卡

- 行动卡过去会强行选一位“赢家”，没有综合整场最有效的练习路径。
- 卡片文字过多，扫描成本高。
- 会补写用户没说过的经历，例如默认用户“被说听不懂”。
- 某些观察指标像在考听者。反馈请求应自然，例如询问“你理解到的重点是什么、哪里还需要补充”，而不是让听者完成测试。
- 赠言有时是通用格言，不是本场发言的提炼。
- 表达技能题被全局禁止显示历史回声，导致卡片缺少经过核验的作品或原话。

### 2.4 UI

- 首页在某些全屏或超宽窗口下标题溢出、被裁切。需要单独修复响应式约束，不改变现有视觉方向。

## 3. 必须实施的架构修改

请先定位现有实现，再按代码库已有模式完成。命名可根据现有结构微调，但职责不能消失。

### 3.1 Grounded Opening

主持人开场必须区分：

- `QuestionIntent`：用户希望本场完成什么。
- `SupportMode`：用户明确表达了哪些情绪或经历；没有就不补。
- `InternalTension`：仅供 Director 分配任务，不能直接冒充用户意图展示。

开场默认两句话：

1. 用用户已经说出的内容，简洁说明本场要讨论的问题。
2. 介绍全部入席者的稳定观察角度，不公开内部详细任务。

表达技能场景的三位稳定角度：

- 李清照：内容与字句。
- 简·奥斯汀：听者视角，或听者与关系。
- 阿达·洛夫莱斯：结构与练习。

稳定角度来自人物作品、思维和人格，但不能声称真人经历过用户的相同处境。

参考语气，不要硬编码成唯一台词：

```text
你想知道，怎样练习才能让认真准备的内容更容易被听懂。
今天，李清照从内容与字句、简·奥斯汀从听者视角、阿达·洛夫莱斯从结构与练习出发。
```

### 3.2 Language Editor

增加清晰的语言编辑层，至少覆盖主持人、卡片，以及触发清晰度风险的先行者发言。

职责：

1. 事实边界：不能添加原文没有的感受、经历、行为、关系和目标。
2. 中文语法：检查杂糅句式、主宾搭配、指代不明和语序问题。
3. 自然表达：减少抽象名词堆叠、产品术语、翻译腔、绕口比喻和生硬转折。
4. 对话衔接：新句要承接当前问题或前文新增信息，不能只靠“换个角度”“我想先”等套话。
5. 权限边界：编辑层只能改写表达和顺序，不能新增判断、建议或人物经历。

编辑后必须重新经过 Output Guard。不要把 Language Editor 做成一张无限增长的禁用词表；确定性规则只拦截高置信度问题，其余作为 repair 指令和评测信号。

### 3.3 Pioneer Turn Planning

生成每轮发言前，先形成内部结构化计划：

- 当前用户最新意图。
- 本轮要新增的唯一价值。
- 与前文的关系：独立、补充、推进、澄清、质疑、让步或收束。
- 不能重复的已有判断、动作和措辞。
- 当前人物的稳定视角、语气和专属思维动作。

要求：

- 不强制每次提到上一位姓名，也不强制复述对方观点。
- 可以自然称“清照”“简”“阿达”，但只有真实互动时才称呼。
- 同意或反对可以出现，但不是固定开头。
- 不强制交锋；根据问题和对话状态选择递进、补充、澄清、碰撞或跳过。
- 发言以语义完整为准。总长度可在合理范围内浮动，必要时拆为一至两个气泡；不要用硬截断制造半句话。

### 3.4 Follow-up Intent 与二次去重

修复追问状态：

- `question`：回答问题。
- `reflection`：回应用户的新理解并推进半步。
- `commitment`：支持选择，补充执行条件。
- `correction`：承认具体误读后回到用户原意，不做过度道歉。
- `closure`：简短确认，并由主持人提供“生成行动卡 / 继续聊”。

重复检测至少覆盖：

- 当前人物历史发言。
- 其他人物本场发言。
- 主持人分析和开场中已经说过的内容。
- 当前生成内容与 repair/fallback 替换后的内容。

如果初次输出碰撞，先 repair；如果进入 fallback，fallback 生成后必须再次进行语义去重与贴题检查。不能用另一句已出现过的固定 fallback 替换。

### 3.5 人物专属 fallback

三位样板必须按 `QuestionIntent + 当前对话缺口` 选择人物专属思维动作，不是固定台词：

- 李清照：识别真正想说的核心、字句轻重、删减后的本意。
- 简·奥斯汀：听者已知信息、关系位置、对方需要先理解什么。
- 阿达·洛夫莱斯：把目标拆成可观察步骤、一次只改一个变量、反馈如何回到下一版。

fallback 仍必须回答用户最新问题，不得因为人物标签把所有问题拉回同一种建议。

### 3.6 Action Card

- 将“本轮选择”稳定为“本轮练习路径”。
- 可以综合多位先行者的互补步骤，不评选单一赢家。
- 每一栏只保留一个清楚动作或判断，显著压缩字数。
- `24 小时 / 7 天 / 30 天 / 行动护栏 / 复盘证据` 必须前后连贯，而不是同一动作换时间重复。
- 所有内容必须来自用户原话与本场谈话，禁止补写经历。
- 观察指标优先使用用户自己可记录的证据；需要他人反馈时，用自然对话，不把听者当考生。

### 3.7 Quote Card 与 Historical Echo

- 上半部分“本场赠言”必须是该人物本场判断的提炼，具有角色特色，不得使用与本场无关的通用格言。
- 下半部分“历史回声”来自经过核验的原文、作品、章节或演讲及来源。
- 删除“表达技能题一律不显示历史回声”的全局短路。
- 只有语义高度相关且来源完整时才展示；没有合适来源时宁可留空。
- 扩充三位样板的经过核验来源库，使表达、创作、听者关系、结构和练习等主题有合理覆盖。不得让模型凭记忆现场编名言。

### 3.8 首页响应式修复

- 修复超宽或全屏窗口下首页标题溢出、裁切。
- 使用稳定的容器、网格、`max-width`、合理字号和换行约束。
- 不按 viewport width 无限放大字体，不改变现有视觉风格。

## 4. 评测体系同步更新

不要只改生成 Harness，不改评测标准。先补充确定性检查和评测数据结构，不调用模型。

至少覆盖：

1. 读题准确性与未授权推断。
2. 主持人是否介绍全部入席者、是否只呈现稳定角度。
3. 中文语法、自然度、可理解性。
4. 人物区分度与盲测可辨识度。
5. 对话承接是否增加新信息，而非复读。
6. 最新追问意图识别。
7. fallback 率、repair 率、重复率和二次校验结果。
8. 行动卡与整场谈话的相关性、可执行性、简洁度和溯源。
9. 赠言的本场贴合度、人物特色、历史回声来源完整性与语义相关性。
10. 主持人和先行者发言的事实边界。

保留：

- `dev / regression / hidden / ood` 四类题集。
- 确定性检查、LLM 裁判、人工体验三条轨道。
- 同一输出的重复裁判方差记录。
- baseline 与 final 的盲测对照。

把以上三题及其真实失败模式沉淀到 regression，但不要把具体答案写进生成 Prompt。

## 5. 验证与交付

修改完成后运行：

```bash
npm run check:harness
npx tsc --noEmit
npm run build
```

不要运行 `npm run eval -- --full` 或任何完整模型评测。

最后汇报：

1. 修改了哪些根因，分别位于 Harness 哪一层。
2. 哪些问题由确定性检查保证，哪些仍依赖模型生成与评测。
3. 具体修改文件。
4. 三项本地检查结果。
5. 仍需人工复测的三道题和观察点。
6. 剩余风险。

不要提交、合并或推送，除非用户另行明确要求。

## 6. 本轮不做

- 登录、Supabase 历史记录、圆桌恢复和收藏。
- 支付、额度限制、线上部署。
- 大型向量 RAG。
- 一次性扩展全部九位 PioneerMind。

三位样板通过人工回归后，再扩展其余六位。
