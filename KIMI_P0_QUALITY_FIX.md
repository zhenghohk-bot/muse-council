# Muse Council P0 通用质量修复

## 你的角色

你正在接手 Muse Council 的一轮 P0 质量修复。请同时以资深 AI 产品经理、Roundtable Harness Engineer、中文内容编辑和前端产品工程师的视角工作。

项目路径：

```text
/Users/hejennifer/Sites/muse-council
```

当前分支应为：

```text
agent/p2-evaluation-case-study
```

## 0. 最高优先级边界

1. 当前工作区有用户、Codex、Claude Code 共同完成但尚未提交的改动。不得运行 `git reset`、`git checkout`、`git restore`、`git clean`、`git stash`，不得覆盖或删除既有改动。
2. 不读取、打印、修改或提交 `.env.local`。
3. 本文件记录的是最新产品决定；若与 `CLAUDE_NEXT_ITERATION.md`、`CLAUDE_ROUNDTABLE_QUALITY_FIX.md` 的旧规则冲突，以本文件和 `.agents/skills/roundtable-host-editor/` 为准。
4. 不为三道表达题添加关键词特判、固定答案或题目专属分支。必须修复跨主题可复现的通用机制。
5. 不运行完整 API 评测、后台循环或高消耗模型任务。只运行本地确定性检查。需要调用生成模型或裁判模型时先停止并向用户说明。
6. 不提交、不合并、不推送，除非用户另行明确授权。

## 1. 开始方式

先完成以下动作，再直接实施，不要停在泛泛计划：

1. 运行 `git status --short --branch`，确认分支与脏工作树。
2. 阅读：
   - 本文件
   - `.agents/skills/roundtable-host-editor/SKILL.md`
   - `.agents/skills/roundtable-host-editor/references/voice-contract.md`
   - `.agents/skills/roundtable-host-editor/references/examples.md`
   - `lib/harness/stage-generator.ts`
   - `lib/harness/language-editor.ts`
   - `app/card/page.tsx`
   - `app/card/ShareCard.tsx`
   - `scripts/check-harness-contracts.ts`
3. 用 5 至 8 行复述：四个根因分别位于哪一层、准备按什么顺序修改。
4. 随后直接修改代码与确定性测试。

## 2. 产品基准

圆桌应像一场有温度、有思想、可以继续追问的谈话，而不是咨询报告、会议议程或 AI 工作流。

主持人的职责只有：

1. 确认先行者已经入席。
2. 在不添加新判断的前提下，轻轻承接用户明确说出的主题。
3. 把话交给圆桌。

用户已经认可的语气参考：

```text
三位先行者已经入席。关于表达这件事，不妨先听听她们怎么想。
```

这是语义参考，不是所有主题都强制套用的唯一模板。

## 3. 必须完成的四个 P0

### P0-1 主持人开场机制

删除面向用户的固定报幕和任务分工，包括但不限于：

- `李清照看……`
- `简·奥斯汀看……`
- `阿达看……`
- `某某从……角度`
- `本场要讨论的是……`
- `本轮围绕……`
- `负责……`
- `会帮你……`

Director 内部仍然可以基于人物能力与本场意图分配视角，但不得把内部任务分工机械地展示给用户。

开场要求：

- 不必逐一报出人物姓名。
- 不列人物角色、角度或工作包。
- 只使用用户明确说出的主题、情绪与经历。
- 不把 `InternalTension`、模型分析或推断冒充用户意图。
- 不虚构人物有过和用户相同的经历。
- 语言自然、短而有余地，不急着解决问题。

请同时检查并同步：

- 生成 prompt。
- fallback opening。
- Language Editor / Output Guard。
- `check:harness` 中的回归契约。

### P0-2 通用语言自然度

不要只替换几句坏文案，也不要把 Language Editor 做成无限增长的禁词表。

目前真实失败样例：

- `耳朵最先通向哪里`：主谓关系和语义搭配不自然，为了文学感强造比喻。
- `具体经历只是证据`：指代不清、概念跳跃，用户不知道“具体经历”具体指什么。
- `对方要用的那一样`、`从你的话里拿走什么`：翻译腔、产品术语感强。

Language Editor 的通用检查与 repair 指令至少覆盖：

1. 指代对象是否明确，代词能否回指到具体内容。
2. 主谓宾、修饰语和搭配是否自然。
3. 抽象概念之间是否缺少必要的解释或过渡。
4. 普通用户能否第一次读懂，不需要反推作者想表达什么。
5. 比喻是否真的帮助理解；若只是制造文学感，应改为自然直述。
6. 是否出现翻译腔、产品术语、咨询话术或生造表达。
7. 后一句是否自然承接当前问题与前文新增信息。

边界：

- Language Editor 只能调整措辞、语序、指代与衔接，不能新增事实、情绪、建议或人物经历。
- 修改后必须重新经过事实边界、贴题与重复检查。
- 高置信度错误可确定性拦截；其他问题进入 repair 和质量评测，不要全部硬编码。

### P0-3 金句卡默认选择逻辑

当前 UI 会默认把前两位先行者加入组合，造成明显的位置偏差。

修改为：

- 初始状态不默认选择任何金句卡。
- 用户主动点击“加入组合”后才进入组合。
- 点赞/喜欢状态与“加入组合”是两个独立状态。
- 列表顺序、人物位置、是否存在历史回声，都不得自动改变组合选择。
- 刷新或重新生成卡片时，不应无意恢复“前两张默认选中”。如有持久化逻辑，必须只恢复用户明确选择过的状态。
- 空选择状态下，组合保存按钮应有合理的禁用或提示行为，不能偷偷代选。

请补充可执行的组件逻辑测试；若项目没有 UI 测试框架，至少把纯选择规则抽成可确定性验证的函数，并纳入现有检查或轻量测试。

### P0-4 行动卡可理解性与信息密度

当前问题不是只有某一句不好，而是行动卡有时把谈话强行套进抽象模板。

每个行动项必须独立回答：

1. 做什么。
2. 怎么做。
3. 如何知道完成或有效。

规则：

- `本轮练习路径` 综合整场谈话，不评选某位先行者，也不强行套人物句式。
- 不使用概念转换不成立的动作，例如 `选一个词，再把它配成一句“因为……”的判断`。
- 优先给用户可以立刻执行的自然动作，例如：`先写下一句最希望听者记住的结论，再补充两条理解它所必需的信息。`
- 每一栏只保留一个主要动作或判断，降低扫描成本。
- `24 小时 / 7 天 / 30 天 / 行动护栏 / 复盘证据` 要前后递进，不是同一个动作换时间重复。
- 不补写用户没有说过的经历、关系、反馈或困难。
- 反馈任务不能像考试听者；应使用自然请求，例如询问对方理解到的重点、哪里还需要补充。
- 卡片生成、fallback、render 前后的压缩逻辑都要检查，避免某一层重新把内容扩写。

请为行动卡增加确定性质量检查：空泛抽象动作、缺少可执行动词、同栏多任务、跨栏近义重复、未经授权的经历补写。不能可靠硬判的部分写入 repair 指令和后续评测指标。

## 4. 同步保留但不要扩大范围

以下现有能力不得回退：

- `QuestionIntent` 与 `SupportMode` 分离。
- 不补写用户未说出的情绪和行为。
- 追问按最新意图回应。
- fallback 后再次去重和贴题检查。
- 历史回声只使用核验来源，高相关才展示。
- 发言长度以语义完整为先，不用硬截断制造半句话。
- 不强制交锋或强制复述上一位。

首页标题文案 `与她们坐一桌` 不自然已记为 P1 文案待办。本轮不要借机重做首页或视觉系统，除非只需一行、不影响四个 P0 的微小修正；否则留在最终报告的剩余事项中。

## 5. 回归与确定性测试

必须新增或更新确定性检查，至少覆盖：

1. 已认可的自然开场通过。
2. 包含人物逐一分工的旧开场被拦截。
3. `本场要讨论`、`某某看`、`某某从……角度` 等会议报幕被拦截。
4. 主持人没有虚构人物经历和用户未说出的意图。
5. 指代不明、强造比喻、翻译腔样例能触发对应语言问题或 repair。
6. 三张金句卡初始均未加入组合。
7. 点赞不会自动加入组合，加入组合也不会自动点赞。
8. 空组合不能生成一张偷偷包含前两位人物的卡。
9. 行动卡各栏有清楚动作，跨栏不做近义重复。
10. 行动卡不补写用户未提供的经历。

本轮不运行任何付费模型评测。完成修改后只运行：

```bash
npm run check:harness
npx tsc --noEmit
npm run build
```

若检查失败，修复由本轮改动导致的问题；不要通过降低既有门槛让测试变绿。

## 6. 修改后的人工回归题

代码检查通过后，在最终报告中提醒用户人工重测原三题，但不要由你自动调用模型：

```text
1. 如何提升自己的表达能力？
2. 我一到汇报就特别紧张，脑子容易空白，如何提升表达能力？
3. 我认真准备了很多内容，但说出来总让人抓不住重点，应该怎样练习？
```

再补一道分布外问题，避免只对表达题过拟合。你可以建议题目，但不要把题目答案写进 Prompt。

人工重点观察：

- 主持人是否自然开场，不再报幕。
- 角色发言是否第一次就能读懂，指代和逻辑是否清楚。
- 追问是否有新增信息且不复读。
- 行动卡是否更短、更具体、前后连贯。
- 金句卡是否默认全未选，用户选择是否真实生效。

## 7. 最终交付格式

最后请汇报：

1. 四个 P0 各自的根因与修复层级。
2. 修改文件清单。
3. 新增的确定性保护。
4. 三项本地检查结果。
5. 哪些质量仍需人工或 LLM 评测验证。
6. 原三题与一道 OOD 题的人工复测建议。
7. 剩余风险与 P1 待办。

不要声称实际模型体验已改善，除非用户完成回归或另行授权模型评测。
